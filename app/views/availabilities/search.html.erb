<p id="notice"><%= notice %></p>

<div id = "search_page">
    <div id = "search_form">
        <%= render 'searchform' %>
    </div>
    <div id = "result">
        <% if @availabilities %>
            <div id="results">
                <%= render 'result', :availabilities => @availabilities %>
            </div>
        <% else %>
            <h2> no results have been found </h2>
        <% end %>
    </div>
</div>
