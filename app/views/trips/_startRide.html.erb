<% if current_user.is_driver?%>
    <%= link_to 'Get direction with Google Map', "http://maps.google.com/maps?saddr=#{curr_lat},#{curr_lon}&daddr=#{dest_lat},#{dest_lon}", :target => '_blank', class: "btn btn-info" %>
    <%= link_to 'Trip Completed', trip_complete_path, class: "btn btn-primary"%>
<% end %>