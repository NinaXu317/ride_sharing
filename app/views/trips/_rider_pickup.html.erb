<div id="rider_pickup", style="margin-left:10%; margin-top:5%">
  <div id="rider_pickup_buttons", style="margin-top:5%">
    <%= link_to 'Direction', "http://maps.google.com/maps?saddr=#{curr_lat},#{curr_lon}&daddr=#{dest_lat},#{dest_lon}", :target => '_blank', class: "btn btn-info" %>
    <p>If your rider has been picked up, click the following button to start your trip</p>
    <%= link_to 'Rider picked up', pickup_user_trip_path(current_user.id, @trip.id), class: "btn btn-primary"%>
  </div>
</div>